export class Player {
    public id;
    public name;
    public count;

    constructor(id: number, name: string, count: number) {
        this.id = id;
        this.name = name;
        this.count = count;
    }
}